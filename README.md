# Antigravity IDE Self-Learning Repository

This repository serves as the persistent, version-controlled backup of the Antigravity IDE's local `memory` and `skills` core.

## Structure
- `memory/`: Contains contextual memory items grouped by types (user, feedback, project, reference).
- `skills/`: Contains modular AI agent playbooks, bug-fix patterns (TIL), and domain-specific operations that auto-trigger upon relevant tasks.

> **Zero Trust Enforced:** Repositories are strictly audited for secrets before pushing.

*Maintained automatically by ComarAI Antigravity Engine.*
